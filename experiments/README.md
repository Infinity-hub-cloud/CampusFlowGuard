# 实验目录

- `baseline/`：论文基线复现实验记录。
- `innovation/`：独立创新实验记录。

每次实验后续应保存不可变实验 ID、配置快照、Git commit、随机种子、数据哈希、划分索引哈希、特征列表、阈值、混淆矩阵、完整指标、参数量、训练时间和推理吞吐量。生成的大文件放入被 `.gitignore` 排除的 `runs/` 或 `outputs/`。

当前阶段没有实验结果。
